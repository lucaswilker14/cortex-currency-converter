name: Test Get Actions GitHub API

on:
  workflow_dispatch:
  
  repository_dispatch:
    types:
      - test-actions-api

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Testando Aplicação
        run: echo Actions via API deu bom!
