name: Test Get Actions GitHub API

on:
  workflow_dispatch:
  
  repository_dispatch:
    types:
      - webhook

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Testando Aplicação
        run: echo Hello, world!
